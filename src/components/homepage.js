import React from 'react';
import Header from './header';
import Base from './base';

const Homepage = () => {
    return (
      <div>

        <Header title={'title__'}/>
        <Base />

      </div>
    )
}

export default Homepage;
